Given(/^I am on a project landing page$/) do
  visit project_path(id: 12)
end

When(/^I change the order of the list of tickets$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^the ticket priority values should be updated$/) do
  pending # express the regexp above with the code you wish you had
end